<%- include('partials/header') %>
    <style>
        .profile-section {
            padding: 40px 20px;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .profile-section h2 {
            text-align: center;
            margin-bottom: 30px;
            font-size: 28px;
            font-weight: bold;
            color: #333;
        }
        
        .profile-container {
            display: flex;
            gap: 30px;
        }
        
        .sidebar {
            width: 250px;
            background: #f5f5f5;
            border-radius: 10px;
            padding: 20px;
        }
        
        .sidebar ul {
            list-style: none;
            padding: 0;
        }
        
        .sidebar li {
            margin-bottom: 20px;
        }
        
        .sidebar li a {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            font-size: 16px;
        }
        
        .sidebar li.active a {
            color: #007bff;
        }
        
        .sidebar .icon {
            margin-right: 8px;
        }
        
        .content {
            flex: 1;
            background: #ffffff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        
        .content h3 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #007bff;
            border-bottom: 2px solid #eee;
            padding-bottom: 10px;
        }
        
        .profile-info,
        .change-password {
            margin-top: 30px;
        }
        
        .profile-info h4,
        .change-password h4 {
            margin-bottom: 20px;
            font-size: 20px;
            color: #555;
        }
        
        .info-field {
            margin-bottom: 15px;
        }
        
        .info-field label {
            font-weight: 600;
            display: block;
            margin-bottom: 5px;
            color: #555;
        }
        
        .info-field p,
        .info-field input {
            background: #f8f9fa;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ddd;
            width: 100%;
            font-size: 16px;
        }
        
        .save-button {
            background-color: #007bff;
            color: #fff;
            padding: 12px 20px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 10px;
        }
        
        .save-button:hover {
            background-color: #0056b3;
        }
        
        .content-section {
            display: none;
        }
        
        .content-section.active {
            display: block;
        }
    </style>
    <div class="profile-section">
        <h2>TRANG CÁ NHÂN</h2>
        <div class="profile-container">
            <!-- Sidebar -->
            <div class="sidebar">
                <ul>
                    <li class="active">
                        <span class="icon">👤</span>
                        <a href="#account" onclick="showSection('account')">TÀI KHOẢN CỦA TÔI</a>
                    </li>
                    <li>
                        <span class="icon">📦</span>
                        <a href="/order">ĐƠN MUA</a>
                    </li>
                    <li>
                        <span class="icon">🚪</span>
                        <a href="/auth/logout">ĐĂNG XUẤT</a>
                    </li>
                </ul>
            </div>

            <!-- Nội dung chính -->
            <div class="content">
                <!-- Phần Tài khoản của tôi -->
                <div id="account" class="content-section active">
                    <h3>TÀI KHOẢN CỦA TÔI</h3>

                    <!-- Hồ sơ -->
                    <form id="profile-form" class="profile-info">
                        <h4>HỒ SƠ</h4>

                        <div class="info-field">
                            <label for="user_name">TÊN ĐĂNG NHẬP</label>
                            <input type="text" id="user_name" name="user_name" value="<%= user.user_name %>" readonly />
                        </div>

                        <div class="info-field">
                            <label for="full_name">HỌ TÊN</label>
                            <input type="text" id="full_name" name="full_name" value="<%= user.full_name || '' %>" placeholder="Nhập họ tên..." />
                        </div>

                        <div class="info-field">
                            <label for="email">EMAIL</label>
                            <input type="email" id="email" name="email" value="<%= user.email %>" readonly />
                        </div>

                        <div class="info-field">
                            <label for="address">ĐỊA CHỈ</label>
                            <input type="text" id="address" name="address" value="<%= user.address || '' %>" placeholder="Nhập địa chỉ..." />
                        </div>

                        <div class="info-field">
                            <label for="phone">SỐ ĐIỆN THOẠI</label>
                            <input type="text" id="phone" name="phone" value="<%= user.phone || '' %>" placeholder="Nhập số điện thoại..." />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    </div>

    <script>
        function showSection(sectionId) {
            // Ẩn tất cả các section
            document.querySelectorAll('.content-section').forEach(section => {
                section.classList.remove('active');
            });
            // Xóa class active khỏi tất cả các mục trong sidebar
            document.querySelectorAll('.sidebar li').forEach(li => {
                li.classList.remove('active');
            });

            // Hiển thị section được chọn
            document.getElementById(sectionId).classList.add('active');
            // Thêm class active cho mục được chọn trong sidebar
            document.querySelector(`.sidebar a[href="#${sectionId}"]`).parentElement.classList.add('active');
        }

        function getCookie(name) {
            const value = `; ${document.cookie}`;
            const parts = value.split(`; ${name}=`);
            if (parts.length === 2) return parts.pop().split(';').shift();
            return null;
        }

        // Kiểm tra cookie error
        const error = getCookie('error');
        if (error) {
            const errorMessage = document.getElementById('error-message');
            errorMessage.style.display = 'block';
            errorMessage.textContent = error;
            document.cookie = 'error=; Max-Age=0; path=/';
            setTimeout(() => {
                errorMessage.style.display = 'none';
            }, 5000);
        }

        // Kiểm tra cookie success
        const message = getCookie('message');
        if (message) {
            const successMessage = document.getElementById('success-message');
            successMessage.style.display = 'block';
            successMessage.textContent = message;
            document.cookie = 'message=; Max-Age=0; path=/';
            setTimeout(() => {
                successMessage.style.display = 'none';
            }, 5000);
        }

        // Xử lý form đổi mật khẩu
        document.getElementById('change-password-form') ? .addEventListener('submit', async function(e) {
            e.preventDefault();
            const form = e.target;
            const currentPassword = form.currentPassword.value;
            const newPassword = form.newPassword.value;
            const confirmPassword = form.confirmPassword.value;

            if (newPassword !== confirmPassword) {
                document.cookie = 'error=Mật khẩu xác nhận không khớp.; Max-Age=5; path=/';
                window.location.reload();
                return;
            }

            try {
                const response = await fetch('/auth/profile/change-password', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        currentPassword,
                        newPassword,
                    }),
                });

                if (response.ok) {
                    document.cookie = 'message=Đổi mật khẩu thành công!; Max-Age=5; path=/';
                    window.location.reload();
                } else {
                    const errorText = await response.text();
                    document.cookie = `error=${errorText}; Max-Age=5; path=/`;
                    window.location.reload();
                }
            } catch (err) {
                document.cookie = 'error=Không thể kết nối đến máy chủ.; Max-Age=5; path=/';
                window.location.reload();
            }
        }); <
        script >
            document.getElementById('profile-form').addEventListener('submit', async function(e) {
                e.preventDefault();

                const updatedUser = {
                    full_name: document.querySelector('[name="full_name"]').value,
                    address: document.querySelector('[name="address"]').value,
                    phone: document.querySelector('[name="phone"]').value
                };

                try {
                    const response = await fetch('/auth/updateprofile', {
                        method: 'PUT',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify(updatedUser),
                    });

                    if (response.ok) {
                        document.cookie = 'message=Cập nhật hồ sơ thành công!; Max-Age=5; path=/';
                        window.location.reload();
                    } else {
                        const errorText = await response.text();
                        document.cookie = `error=${errorText}; Max-Age=5; path=/`;
                        window.location.reload();
                    }
                } catch (err) {
                    document.cookie = 'error=Không thể kết nối đến máy chủ.; Max-Age=5; path=/';
                    window.location.reload();
                }
            });
    </script>

    </script>

    <%- include('partials/footer') %>